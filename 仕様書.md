# ポートフォリオサイト 実装仕様書

> この文書は Claude Code に実装を依頼するための仕様書です。
> 「なぜそうするか」を各所に併記しています。実装中に判断に迷ったら、その理由に照らして決めてください。
> 理由と矛盾する指示があれば、指示より理由を優先し、その旨を報告してください。

---

## 0. このプロジェクトの一行要約

**制作物を「夜のビルの窓」として見せる、黒と紫のポートフォリオサイト。**
派手な演出を持ちながら Lighthouse 95+ を維持することそのものが、このサイトの主張である。

---

## 1. 前提と原則

### 1.1 中核となる設計原則

サイト全体を貫く原則が1つあります。**すべての判断はこれに従ってください。**

> **闇が主役で、光は例外である。**

これは筆者のアイコン（`#000000` と `#4805F1` の2色のみ、面積比 94.7% : 4.5%）から導出したものです。
実際の夜の都市も同じで、視界のほとんどは暗闇であり、光は一点に集中しているから強く見えます。

具体的な帰結:

- 発光している要素の**画面占有面積は 25% 以下**に保つ
- 光は**一点に集めて強く**する。同じ光量を全面にばらまかない
- グローは「光っているもの」にだけ付ける。地の面や文字組み全体には広げない

> **改訂（2026-08-11）: ネオンを解禁しました**
>
> 当初この節は「発光面積10%以下」「グローは原則禁止（例外1箇所のみ）」と定めており、
> 結果として**サイト全体でホバー中の1セルしか光らない**状態になっていました。
> ネオン街をイメージして始めたのに、サイト自身の要素にネオンが一切ない状態です。
>
> ただし当初の**理由**は今も有効です。禁じたかったのは
> 「のべつまくなしに光らせること」（下表の「全要素へのグロー」）であって、
> ネオンそのものではありません。**集中した意図的なネオンはこの理由と矛盾しません。**
>
> **やってはいけない代償行為**: 光を足すために周囲をさらに暗くすること。
> 現状すでに十分暗く、これ以上落とすと見づらくなるだけです。
> 明るさの帳尻を合わせるのではなく、**光を集める場所を選ぶ**ことで密度を作ってください。

### 1.2 絶対に避けるアンチパターン

「黒背景 + ネオン」は現在もっともテンプレート化した方向性です。以下が揃うと即座に「よくあるやつ」になります。**実装してはいけません。**

| 禁止 | 理由 |
|---|---|
| 全要素へのグロー（`box-shadow: 0 0 20px`） | ネオンの本質は光量ではなく光の集中。全部光ると何も光っていないのと同じ |
| 遠近感のあるグリッド床 / ワイヤーフレーム地平線 | Synthwave の記号。2015年頃から使い古されている |
| グリッチ / RGBずらし / スキャンライン | 同上。文字が読めなくなる実害もある |
| シアン × マゼンタを**主役級の面積で**併用する | サイバーパンクの定型配色。本サイトの紫が死ぬ |
| 降りしきる雨、浮遊パーティクル、Matrix風の文字列 | 主題と無関係な既視感の塊 |
| `Orbitron` / `Rajdhani` などのSF系フォント | 一発で安っぽくなる |
| カーソル追従のカスタムカーソル（大きな円が遅れて追ってくるやつ） | ポートフォリオの定番演出になりすぎている |

> **配色についての改訂（2026-08-11）**
>
> **ネオンの配色に紫縛りはありません。** 色はバランスで判断してください。
> 実際の繁華街の看板は多色で、紫だけのネオン街は存在しません。
>
> 上表の「シアン × マゼンタ」が禁じているのは、
> **その2色だけを主役級の面積で対にする**という、サイバーパンクの定型配色です。
> 多色の中の一部としてシアンやマゼンタを使うことは問題ありません。
>
> 判断基準:
> - 1つの色が全体を支配していないか（紫を含む。紫一色も避ける）
> - 隣接する管の色が喧嘩していないか
> - 文字として読ませるものは §1.3 のコントラスト（4.5:1）を満たしているか
>
> `--c-signal` は依然サイトのアイデンティティですが、
> 「他の色を使わない理由」ではなくなりました。

### 1.3 品質の床（交渉不可）

以下は「余裕があれば」ではなく**必須要件**です。これを割るくらいなら演出を削ってください。

- モバイル（375px）まで完全に破綻しない
- キーボードのみで全機能が操作可能、フォーカスリングが視認できる
- `prefers-reduced-motion: reduce` で全アニメーションが停止する
- JavaScript が実行されなくても、全作品が閲覧できる
- 本文テキストのコントラスト比 4.5:1 以上

---

## 2. 技術スタック

| 層 | 採用 | バージョン | 理由 |
|---|---|---|---|
| フレームワーク | **Astro** | 5.x | 静的HTMLをビルド時に出力し、JSは必要な箇所だけ島として載る。演出と速度の両立に最適 |
| アニメーション | **GSAP + ScrollTrigger** | 3.13+ | 2025年4月に全プラグイン無償化済み。スクロール演出のデファクト |
| WebGL | **OGL** | 1.x | Three.js（約150KB）に対し約10KB。背景シェーダーにしか使わないので軽さを優先 |
| スタイル | **素のCSS**（カスケードレイヤー + カスタムプロパティ） | — | Tailwind は使わない。デザイントークンを一次情報として持ちたいのと、ユーティリティクラスの羅列が「このサイトについて」ページで説明しづらいため |
| ホスティング | **Cloudflare Workers**（Static Assets） | — | 静的配信で十分だが、Pages より機能の幅が広く、今なら移行コストがゼロ。詳細は下記 |
| 型 | **TypeScript**（`strict: true`） | 5.x | — |

### 2.1 明示的に採用しないもの

- **Next.js** — コンテンツが本質的に静的で、サーバー機能が不要。Astro のほうが出力JSが少ない
- **Tailwind CSS** — 上記の通り
- **Lenis（慣性スクロール）** — 導入は**しない**。ネイティブスクロールを乗っ取るため INP に悪影響があり、トラックパッド以外での操作感が悪化する。「気持ちよさ」の対価としてコアウェブバイタルを払う価値がないと判断
- **React / Vue** — アイランドの中身も Astro コンポーネント + 素のTSで足りる。ランタイムを載せる理由がない
- **Cloudflare Pages** — 当初はこちらを指定していたが、Workers（Static Assets）に変更した（2026-08-09 改訂）

> **判断の記録**: Lenis の不採用は「このサイトについて」ページで明示的に触れること。採用しなかった選択肢を語れることが、このサイトの差別化要素そのものです。

> **ホスティング変更の経緯（2026-08-09 追記）**
>
> 当初の「Cloudflare Pages — 静的配信で十分」という理由付け自体は誤っていません。
> このサイトは完全な静的出力なので、Pages でも過不足なく動きます。
>
> 変更したのは、両者の差が「後から動的処理が必要になったときだけ」に集約されるためです。
> Pages では Cron Triggers、Queue、Workers Logs、Email Workers、非ルートパスへの配信が使えず、
> 必要になった時点で移行作業が発生します。
> Pages の実質的な優位だった Git 連携とPRごとのプレビューデプロイは、
> Workers Builds が同等に提供するようになり、差がなくなりました。
>
> 静的配信の結果は両者で同じで、パフォーマンス数値も変わりません。
> 選んでいるのは速度ではなく将来の選択肢の広さで、代償は `wrangler.jsonc` 一枚です。
>
> なお Cloudflare は Pages を非推奨にしていません（廃止の告知はなく現役のプロダクトです）。
> 「Pages が畳まれるから」ではなく「Workers のほうが守備範囲が広いから」が理由です。

---

## 3. デザイントークン

`src/styles/tokens.css` に定義し、**すべてのスタイルはここを参照する**こと。生の16進数をコンポーネント内に書かないでください。

### 3.1 カラー

```css
:root {
  --c-void:         #000000;  /* 画面の大半。純黒 */
  --c-facade:       #0B0A14;  /* 面・カード。わずかに紫に振った黒 */
  --c-facade-lit:   #1A0740;  /* 微光。作品セルの待機状態 */
  --c-signal:       #4805F1;  /* 主役。アイコンと同一 */
  --c-fluorescent:  #F2F0FF;  /* 見出し。蛍光灯の青白さ */
  --c-dust:         #8B87A3;  /* 本文。紫を落とした灰 */
  --c-sodium:       #FF7A18;  /* ナトリウム灯。1ページ1箇所のみ */
}
```

**使用ルール**

- `--c-signal` の総面積は各ビューポートで **10% 以下**
- `--c-sodium` は **1ページに1箇所だけ**。使い所は §5 で指定
- 中間のグレーを新規に足さない。暗さは `--c-void` と `--c-facade` の2段階で表現する
- ダークモード切替は**実装しない**。このサイトは常に暗い

### 3.2 タイポグラフィ

3書体を役割で使い分けます。**すべてセルフホスト**（`public/fonts/`）し、Google Fonts へのランタイム接続は行わないこと（third-party接続はLCPを悪化させます）。

| 役割 | 書体 | 用途 | 補足 |
|---|---|---|---|
| Display | **Archivo**（可変フォント） | 大見出し、作品タイトルの欧文 | `wdth` 軸を 75〜85 に絞って使う。街の看板は縦長で圧縮されているため |
| 和文 | **Zen Kaku Gothic New** | 和文の見出しと本文 | Weight 400 / 700 のみ |
| Utility | **IBM Plex Mono** | 制作年、番号、技術スタック名、キャプション | 数字が判別しやすく、データ的な情報に硬質さが出る |

**採用しない書体**: Inter、Space Grotesk、Montserrat、Poppins、Orbitron、Rajdhani。いずれも「とりあえず選ばれる」書体で、固有性に寄与しません。

**サブセット化**: 和文は必須です。`subfont` または `pyftsubset` でビルド時に使用文字だけ抽出し、`font-display: swap` を指定。和文フルセットをそのまま読むと数MBになりLCPが破綻します。

**タイプスケール**（`clamp()` で流動的に）

```css
--fs-display: clamp(2.5rem, 8vw, 7rem);
--fs-h1:      clamp(1.75rem, 4vw, 3rem);
--fs-h2:      clamp(1.375rem, 2.5vw, 2rem);
--fs-body:    1rem;
--fs-small:   0.8125rem;
--fs-caption: 0.75rem;
```

行間は和文本文 `1.9`、欧文見出し `1.05`。和文は行間を広く取らないと暗背景で読めません。

### 3.3 スペーシングとモーション

```css
--space-1: 0.5rem;  --space-2: 1rem;   --space-3: 1.5rem;
--space-4: 2.5rem;  --space-5: 4rem;   --space-6: 8rem;

--dur-instant: 120ms;
--dur-fast:    240ms;
--dur-base:    480ms;
--dur-slow:    900ms;

--ease-out:   cubic-bezier(0.16, 1, 0.3, 1);
--ease-inout: cubic-bezier(0.65, 0, 0.35, 1);
```

**モーション原則**: 点灯は速く（`--dur-fast`）、消灯は遅く（`--dur-base`）。実際の照明は点くのが一瞬で、余韻を残して消えます。この非対称性が「光」の質感を作ります。

### 3.4 角丸

- 窓セル: `2px`（ほぼ直角。建築物の開口部なので）
- カード・パネル: `4px`
- ボタン: `2px`

大きな角丸は使いません。都市の夜景に丸みは似合いません。

---

## 4. ディレクトリ構成

```
.
├── src/
│   ├── components/
│   │   ├── facade/
│   │   │   ├── WorksList.astro        # 作品一覧（静的HTML）
│   │   │   ├── WorkSign.astro         # 作品1件 = 看板1枚
│   │   │   └── ignite.ts              # 点灯シーケンス（GSAP island）
│   │   ├── sky/
│   │   │   ├── LightPollution.astro   # canvas要素の器
│   │   │   ├── sky.ts                 # OGL初期化・遅延ロード
│   │   │   └── sky.frag               # フラグメントシェーダー
│   │   ├── rail/
│   │   │   └── VerticalRail.astro     # 縦組みテキストレール
│   │   ├── motion/
│   │   │   ├── reveal.ts              # 汎用スクロールリビール
│   │   │   └── motionGuard.ts         # reduced-motion 判定の単一窓口
│   │   └── layout/
│   │       ├── Header.astro
│   │       └── Footer.astro
│   ├── content/
│   │   ├── config.ts                  # コレクションスキーマ
│   │   ├── works/                     # 作品（1作品1ファイル）
│   │   └── notes/                     # 「このサイトについて」の意思決定ログ
│   ├── layouts/
│   │   └── Base.astro
│   ├── pages/
│   │   ├── index.astro
│   │   ├── works/[...slug].astro
│   │   └── about-this-site.astro
│   └── styles/
│       ├── tokens.css                 # §3 のトークン
│       ├── reset.css
│       └── base.css
├── public/
│   └── fonts/                         # サブセット済みwoff2
└── astro.config.mjs
```

---

## 5. ページ仕様

### 5.1 トップ（`/`）

上から順に3セクション。

#### (a) ヒーロー

- 画面高 `100svh`（`100vh` ではない。モバイルのURLバーで破綻するため）
- 中央にサイトタイトル / 肩書きを配置
- 背景は `--c-void` のみ。**ヒーローに WebGL は載せない**

> **理由**: ここが LCP 要素です。JSに依存させると、遅延した瞬間に真っ白な画面になります。ヒーローのテキストは静的HTMLで最初から表示され、CSSアニメーションでのみフェードインさせること。`opacity: 0` の初期値をJSで設定してはいけません。

- 演出: 文字を行単位で下からスライドイン。CSS `@keyframes` のみ、`animation-delay` で段差をつける。GSAPは使わない
- 画面下端に控えめなスクロール誘導。`--c-dust`、`--dur-slow` で上下 4px の反復

#### (b) 作品一覧 ← **署名要素**

このサイトで最も重要なセクションです。詳細は §6.1 に分離しました。

#### (c) フッター

- 連絡先とSNS
- **ここが `--c-sodium` の使い所**。連絡先リンク1本だけをナトリウム灯のオレンジにする
- 紫一色の画面の最後に補色に近いオレンジが1点だけ現れる構成。ページ全体で唯一の暖色

### 5.2 作品詳細（`/works/[slug]`）

- 遷移: View Transitions API（Astro の `<ClientRouter />`）。クリックした窓セルが拡大してヘッダー画像になる `view-transition-name` を設定
- レイアウト: 単一カラム、最大幅 `72ch`
- メタ情報（制作年 / 役割 / 技術スタック）は IBM Plex Mono で、本文の左側に縦に並べる
- 画像は Astro `<Image />` で AVIF/WebP 自動変換。ファーストビューの1枚のみ `loading="eager"` + `fetchpriority="high"`、以降は `lazy`
- 演出は最小限。**作品ページの主役は作品であり、サイトではない**

### 5.3 このサイトについて（`/about-this-site`）

このサイトの差別化要素。**手を抜かないでください。**

構成:

1. **実測値セクション** — Lighthouse の4スコア、初期JS転送量（gzip後）、LCP実測値を数値で表示。ビルド時に生成した JSON を読み込んで表示し、手打ちしないこと（陳腐化するため）
2. **意思決定ログ** — `src/content/notes/` の各エントリを時系列で。1エントリの構造:
   - 判断したこと
   - 検討した選択肢（**採用しなかったものを必ず含める**）
   - 決め手
   - 振り返り（後から追記可能）
3. **演出のBefore/After** — 窓グリッドの点灯シーケンスについて、`stagger` と `duration` をその場でスライダーで変更できるデモを埋め込む。このページ自体が作品になる

初期エントリとして最低限これらを書けるようにデータ構造を用意すること:
- なぜ Next.js ではなく Astro か
- なぜ Lenis を入れなかったか
- なぜ Three.js ではなく OGL か
- なぜグローを使わないか

---

## 6. 署名要素の詳細仕様

### 6.1 作品一覧（2026-08-13 全面改訂）

**コンセプト**: 1作品 = 1枚の看板。夜のネオン街を歩いて看板を読むように、名前と姿が最初から見えている。

> **改訂の経緯: 窓グリッドを廃止しました**
>
> 当初この節は「1つの窓 = 1作品」として、3:4 のセルを敷き詰めたグリッドを定めていました。
> 全セルの65〜75%を装飾（消灯セル）で埋め、作品名とサムネイルは
> **ホバー時のみ**、しかもセルの外に出す設計でした。
>
> これが機能していませんでした。
>
> - **静止状態では何も見えない**。暗い四角が18個並ぶだけで、どれが作品か区別できない
> - **タッチ端末には hover が無い**ため、タップするまで内容が分からない
> - 8列（モバイル4列）では1セルが小さすぎて、そもそも作品を見せられない
> - 消灯セルは装飾として働かず、「作品が入っていない空枠」に見えていた
>
> ポートフォリオの仕事は作品を見せることなので、比喩より機能を優先しました。
> 夜の街で実際に目に入るのは窓ではなく**看板**であり、
> 看板はそもそも「読ませるために光っている」ものです。
>
> 装飾セルは全廃しました。「闇が主役」は背景と余白で足りています。

#### 構造

作品だけを縦に積みます。装飾要素は置きません。

```
1行 = 1作品
768px以上:  番号 / サムネイル(240px) / 本文
767px以下:  サムネイル(38vw) / 本文
```

1件に載せる情報:

| 要素 | 書体 | 備考 |
|---|---|---|
| 連番 | IBM Plex Mono | 767px以下では畳む |
| サムネイル | — | `aspect-ratio: 3 / 2`。**常時表示**（ホバー不要） |
| 欧文タイトル | Archivo | **ネオン管にする**（§6.4）。作品ごとに管の色を変える |
| 和文タイトル | Zen Kaku Gothic New | |
| 制作年 / 役割 | IBM Plex Mono | |
| 一行説明 | Zen Kaku Gothic New | 767px以下では畳む（詳細ページで読める） |
| 技術スタック | IBM Plex Mono | 枠付きのタグ |

**管の色**: 作品の並び順に沿って循環させる。リロードしても同じ作品は同じ色になること。
紫縛りはしない（§1.2 改訂）。多色であることが「ネオン街」を作る。

#### 状態と遷移

| 状態 | 見え方 |
|---|---|
| 静止 | 全情報が読める。管は弱く灯る（`--neon-strength: 0.5`）。サムネイルは `opacity: 0.72` |
| ホバー / フォーカス | 管が強く灯り（`1.15`）、サムネイルが `1` になる。行の背景が `--c-facade` に |

点灯は速く（`--dur-fast`）、消灯は遅く（`--dur-base`）。§3.3 のモーション原則に従う。

### 6.2 光害の空（WebGL背景）

**コンセプト**: 都市の夜空は、地上の光が雲に反射して下側だけ明るい。パーティクルでもグリッドでもなく、この「滲み」だけを描く。

#### 仕様

- 描画対象: 窓グリッドセクションの**背後のみ**。ヒーローとフッターには出さない
- 実装: OGL でフルスクリーン三角形1枚 + フラグメントシェーダー
- シェーダーの内容:
  - 画面下端から上に向かって `--c-signal` → `--c-void` へ減衰するグラデーション
  - 低周波の値ノイズ（fBm、オクターブ3程度）でゆっくり揺らす
  - 時間係数は非常に遅く（`u_time * 0.02` 程度）。「呼吸している」と気づかれない速度
  - **マウス追従はしない**。空は人間に反応しません
- ピクセル密度: `Math.min(devicePixelRatio, 1.5)` で上限を設ける
- 色は必ず `--c-signal` / `--c-void` を JS 側で読み取って uniform に渡すこと。シェーダー内に16進数を直書きしない

#### ロード条件（すべて満たしたときのみ初期化）

1. `IntersectionObserver` でグリッドセクションが視界に入った
2. `requestIdleCallback` でメインスレッドが空いた（未対応ブラウザは `setTimeout(300)`）
3. `prefers-reduced-motion: reduce` **ではない**
4. `navigator.connection.saveData` が `true` **ではない**

条件を満たさない場合、canvas は初期化せず、CSS の静的な `linear-gradient` が代替として残る。

- タブが非アクティブのとき（`document.hidden`）は `requestAnimationFrame` を停止すること

### 6.3 縦組みレール

**コンセプト**: 日本の街の看板は縦書き。海外のポートフォリオテンプレートが構造的にやらない要素。

- 画面右端の縦組みテキスト帯
- `writing-mode: vertical-rl;` `font-family` は IBM Plex Mono
- 内容: 現在地を示す短いテキスト（例: `SELECTED WORKS — 2024/2026`）
- 本文より**遅い速度**でスクロール（parallax 係数 0.6 程度）。GSAP ScrollTrigger の `scrub` を使用
- **768px 未満では非表示**（`display: none`）。狭い画面で本文を圧迫するため
- **ネオン管の縦看板として光らせる**（§6.4 の描き方に従う）

> **改訂（2026-08-11）: 抑制をやめました**
>
> この節には以前「あるかないか分からないくらいで正解。目立たせたくなっても濃度を上げないこと」
> という指示があり、`--c-dust` のほぼ見えない濃度に抑えていました。
>
> 日本の街の縦看板という着想は、**ネオンにしてこそ活きる**要素です。
> 現状ネオンが不足しているサイトで、最も自然にネオン管を置ける場所がここでした。
> よって抑制指示を撤回し、光らせます。
>
> ただし**本文の可読性を邪魔しないこと**は維持します。本文の領域に被せない、
> 狭い画面では消す、`prefers-reduced-motion` では点滅させない。

### 6.4 ネオン管（2026-08-11 追加）

ネオンを足すときは、**必ずこの節の描き方に従ってください。** 各箇所で好きに光らせると、
サイト全体でネオンの質感がばらつき、安っぽくなります。

#### 管の構造

実物のネオン管は「ガラス管の中で光るガス」なので、光り方に層があります。
これを4層で再現します。トークンは `src/styles/neon.css` に定義します。

| 層 | 役割 | 実装 |
|---|---|---|
| 芯 | 管の中心。最も明るく、**ほぼ白に近い** | 文字色そのもの（彩度を落とした明色） |
| 内側グロー | 管のすぐ外の濃い発色 | `text-shadow` / `box-shadow` の小さい blur |
| 外側グロー | 空気中への滲み | 同じ色で大きい blur、低い不透明度 |
| 漏れ光 | 背後の壁が照らされる | 親要素側の淡い `radial-gradient` |

**芯を管の色そのままにしないこと。** 実物は中心が白飛びしていて、色は周囲に出ます。
中心まで濃い色だと「文字に影を付けただけ」に見えます。

#### やってはいけないこと

- **地の面や本文全体に広げる** — §1.2 の「全要素へのグロー」に該当します
- **1色だけで全部の管を作る** — 街の看板は多色（§1.2 改訂）
- **明るさの帳尻合わせに周囲を暗くする** — §1.1 改訂。すでに十分暗いです
- **点滅を主張させる** — 接触不良の演出は「1箇所だけ・不規則・低頻度」。全部が点滅すると安い

#### 交渉不可の制約

ネオンは装飾です。§1.3 の品質の床を割ってはいけません。

- `prefers-reduced-motion: reduce` で**点滅・呼吸を完全に停止**する（発光自体は残してよい）
- 文字として読ませる管は**コントラスト 4.5:1 以上**。グローは背景を明るくするので、実測して確認する
- JSに依存しない。CSSだけで成立させる
- ファーストビューに置く場合は §8 の予算を実測で確認する（LCPの余裕は小さい）

## 7. コンテンツのデータ構造

`src/content/config.ts`:

```ts
import { defineCollection, z } from 'astro:content';

const works = defineCollection({
  type: 'content',
  schema: ({ image }) => z.object({
    title:     z.string(),           // 和文タイトル
    titleEn:   z.string().optional(),// 欧文タイトル（Archivo で表示）
    year:      z.number(),
    roles:     z.array(z.string()),  // 例: ['設計', '実装']
    stack:     z.array(z.string()),  // 例: ['Astro', 'GSAP']
    summary:   z.string().max(120),  // 一覧のホバー時に出る一文
    thumbnail: image(),              // 窓セル内に表示
    cover:     image(),              // 詳細ページのヘッダー
    url:       z.string().url().optional(),
    order:     z.number(),           // グリッド内の並び順（小さいほど先）
    draft:     z.boolean().default(false),
  }),
});

const notes = defineCollection({
  type: 'content',
  schema: z.object({
    title:       z.string(),
    date:        z.date(),
    decision:    z.string(),           // 何を決めたか
    alternatives: z.array(z.object({   // 検討した他の選択肢
      option: z.string(),
      why_not: z.string(),
    })),
    verdict:     z.string(),           // 決め手
  }),
});

export const collections = { works, notes };
```

`draft: true` の作品は本番ビルドから除外すること。

---

## 8. パフォーマンス予算

**この数値を満たさない実装はマージ不可**とします。「このサイトについて」ページで公開する数値なので、達成できないなら演出を削ってください。

| 指標 | 目標 | 測定 |
|---|---|---|
| Lighthouse Performance | 95 以上 | モバイル、スロットリング有 |
| Lighthouse Accessibility | 100 | — |
| LCP | 2.0s 以下 | — （「このサイトについて」のみ 2.4s。下記） |
| CLS | 0.05 以下 | — |
| INP | 200ms 以下 | — |
| 初期JS（gzip後） | **30KB 以下** | WebGLとGSAPは遅延なので初期に含まない |
| フォント合計 | 200KB 以下 | サブセット後 |

> **LCPの例外（2026-08-12 追記）: 「このサイトについて」だけ 2.4s**
>
> このページは和文の分量が突出しており、LCP要素である導入文が
> 和文フォントの到着を待つため 2.25s 前後で頭打ちになっている。
>
> 試して**効かなかった**手（いずれも実測）:
> - 和文Regularを preload する → 2254ms から 2255ms。変化なし
> - 導入文の文字を core サブセットへ寄せる → 2253ms。変化なし
> - タイトルを大きくしてLCP要素を欧文に移す → 導入文の方が面積が大きく要素が変わらず
>
> 依存関係ではなく、ページ全体の和文（約96KB）が帯域を奪い合っているのが実態と見ている。
>
> **内容を削ればすぐ通るが、それはこのページの存在価値を削ることになる**ため、
> 例外として閾値を上げた。代わりに CI では**このページだけ** 2400ms で判定し、
> 他のページは 2.0s のまま落とす（`lighthouserc.json` の assertMatrix）。
> 全体を緩めてはいない。
>
> トップページは 1.81s で、入口の速度は落としていない。

**CLS対策**: 窓グリッドのセルは `aspect-ratio` で高さが確定するため、画像読み込みによるレイアウトシフトは起きないはず。すべての `<img>` に `width` / `height` を明示すること。

**CI**: GitHub Actions で PR ごとに Lighthouse CI を回し、予算割れでビルドを落とす設定を入れてください。同時に、測定結果を `src/data/metrics.json` に書き出し、「このサイトについて」ページから参照できるようにすること。

---

## 9. 実装フェーズ

上から順に進めてください。**各フェーズの終わりに必ず Lighthouse を測定**し、数値を記録してから次に進むこと。

| # | 内容 | 完了条件 |
|---|---|---|
| 1 | Astro 初期化、トークン定義、フォントのサブセット化とセルフホスト | 何も演出のない状態で Lighthouse 100 |
| 2 | Content Collections + 作品ページのルーティング | 全作品が静的HTMLで閲覧できる |
| 3 | 窓グリッド（**静的部分のみ**。JSなし） | JSを無効にしても全作品にアクセスできる |
| 4 | 点灯シーケンス（GSAP アイランド） | 初期JS 30KB以下を維持。reduced-motion で停止 |
| 5 | View Transitions によるページ遷移 | 遷移中に CLS が発生しない |
| 6 | 光害の空（OGL） | 遅延ロード条件4つすべてが機能することを確認 |
| 7 | 縦組みレール | 768px 未満で消えること |
| 8 | 「このサイトについて」ページ + Lighthouse CI | metrics.json が自動更新される |

> フェーズ3を終えた時点で、**サイトとしては完成している**べきです。4以降はすべて上乗せであり、削っても機能を失いません。この構造を崩さないでください。

---

## 10. 実装時の判断ガイド

迷ったときの優先順位:

1. **JSなしで壊れないか** — 壊れるなら設計が間違っている
2. **§1.1 の面積原則を守っているか** — 光を足す前に闇を増やせないか
3. **§1.2 のアンチパターンに触れていないか**
4. **§8 の予算内か**
5. その上で、面白いか

新しい演出を思いついた場合、それが署名要素（窓グリッド）より目立つなら**入れないでください**。ココ・シャネルの助言に従います。出かける前に鏡を見て、アクセサリーを1つ外すこと。

---

## 付録: 参考にすべきでないもの

実装中に参考サイトを探す場合、Awwwards の Site of the Day を漁るのは避けてください。あの層は「1年後に古く見える演出」の見本市であり、本仕様のアンチパターン（§1.2）の供給源でもあります。

代わりに参照すべきは、**実際の夜の都市の写真**です。窓が何割点いているか、光がどこに集中しているか、空がどう滲んでいるかを観察したほうが、このサイトには直接効きます。
